{
  "name": "friday",
  "version": "1.0.0",
  "description": "ai-assistant",
  "main": "app.ts",
  "scripts": {
    "start": "bun run app.ts",
    "dev": "bun --watch app.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@openai/agents": "^0.0.5",
    "bun-types": "latest",
    "dotenv": "^16.5.0",
    "twilio": "^5.7.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}